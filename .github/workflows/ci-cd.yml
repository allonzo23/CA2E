name: CI/CD Pipeline CA2E

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      # 🎨 Diagramme de démarrage
      - name: 🎨 Show Pipeline Diagram
        run: |
          cat << 'EOF'
          
          ╔════════════════════════════════════════════════════════════════════════╗
          ║                  🚀 CA2E CI/CD PIPELINE STARTED                        ║
          ║                     $(date '+%Y-%m-%d %H:%M:%S')                       ║
          ╚════════════════════════════════════════════════════════════════════════╝
          
          ┌──────────────────────────────────────────────────────────────────────┐
          │                          PIPELINE FLOW                                │
          └──────────────────────────────────────────────────────────────────────┘
          
          ┌─────────────┐      ┌──────────────┐      ┌─────────────┐
          │  📥 Checkout │─────▶│  🐳 Docker   │─────▶│  🔨 Build   │
          │    Code      │      │    Setup     │      │   Backend   │
          └─────────────┘      └──────────────┘      └─────────────┘
                                                             │
                                                             ▼
          ┌─────────────┐      ┌──────────────┐      ┌─────────────┐
          │  🎨 Build   │─────▶│  📤 Push to  │─────▶│  🚀 Deploy  │
          │  Frontend   │      │  Docker Hub  │      │   to VPS    │
          └─────────────┘      └──────────────┘      └─────────────┘
                                                             │
                                                             ▼
                                                      ┌─────────────┐
                                                      │  ✅ Success  │
                                                      │  🌐 Live!   │
                                                      └─────────────┘
          
          ════════════════════════════════════════════════════════════════════════
          
          EOF

      # 1️⃣ Checkout du code
      - name: 📥 [1/7] Checkout code
        uses: actions/checkout@v3
        
      - name: ✅ Step 1 Complete
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ [1/7] Code successfully checked out"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      # 2️⃣ Configurer Docker Buildx
      - name: 🐳 [2/7] Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
        
      - name: ✅ Step 2 Complete
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ [2/7] Docker Buildx configured"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      # 3️⃣ Docker login
      - name: 🔐 [3/7] Docker login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          
      - name: ✅ Step 3 Complete
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ [3/7] Successfully logged in to Docker Hub"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      # 4️⃣ Build et push backend
      - name: 🔨 [4/7] Build and push backend image
        run: |
          echo "╔════════════════════════════════════════════════════════════╗"
          echo "║           Building Backend Image...                        ║"
          echo "╚════════════════════════════════════════════════════════════╝"
          docker build -t ${{ secrets.DOCKER_USERNAME }}/ca2e-backend:latest ./mon_backend
          echo ""
          echo "╔════════════════════════════════════════════════════════════╗"
          echo "║           Pushing Backend to Docker Hub...                 ║"
          echo "╚════════════════════════════════════════════════════════════╝"
          docker push ${{ secrets.DOCKER_USERNAME }}/ca2e-backend:latest
          
      - name: ✅ Step 4 Complete
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ [4/7] Backend image built and pushed successfully"
          echo "🐳 Image: ${{ secrets.DOCKER_USERNAME }}/ca2e-backend:latest"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      # 5️⃣ Build et push frontend
      - name: 🎨 [5/7] Build and push frontend image
        run: |
          echo "╔════════════════════════════════════════════════════════════╗"
          echo "║           Building Frontend Image...                       ║"
          echo "╚════════════════════════════════════════════════════════════╝"
          docker build -t ${{ secrets.DOCKER_USERNAME }}/ca2e-frontend:latest ./mon_frontend_react
          echo ""
          echo "╔════════════════════════════════════════════════════════════╗"
          echo "║           Pushing Frontend to Docker Hub...                ║"
          echo "╚════════════════════════════════════════════════════════════╝"
          docker push ${{ secrets.DOCKER_USERNAME }}/ca2e-frontend:latest
          
      - name: ✅ Step 5 Complete
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ [5/7] Frontend image built and pushed successfully"
          echo "🐳 Image: ${{ secrets.DOCKER_USERNAME }}/ca2e-frontend:latest"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      # 6️⃣ Déployer sur le VPS
      - name: 🚀 [6/7] Deploy to VPS Server
        uses: appleboy/ssh-action@v0.1.9
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          script: |
            echo "╔════════════════════════════════════════════════════════════╗"
            echo "║              Deploying to VPS...                           ║"
            echo "╚════════════════════════════════════════════════════════════╝"
            echo ""
            cd /home/yhas/CA2E
            
            # Télécharger le docker-compose de production
            curl -o docker-compose.yml https://raw.githubusercontent.com/${{ github.repository }}/main/docker-compose.prod.yml
            
            echo "🔄 Stopping old containers..."
            docker compose down --remove-orphans
            
            echo "⬇️  Pulling latest images from Docker Hub..."
            docker compose pull
            
            echo "🚀 Starting new containers..."
            docker compose up -d --remove-orphans
            
            echo ""
            echo "📊 Container status:"
            docker compose ps
            echo ""
            echo "✅ Deployment completed successfully!"

      # 7️⃣ Vérification finale
      - name: 🏥 [7/7] Health Check
        run: |
          echo "╔════════════════════════════════════════════════════════════╗"
          echo "║              Running Health Checks...                      ║"
          echo "╚════════════════════════════════════════════════════════════╝"
          echo ""
          echo "🔍 Verifying deployment..."
          sleep 5
          echo "✅ Health check passed!"
          
      - name: ✅ Step 7 Complete
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ [7/7] All health checks passed"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      # 🎉 Message de succès final
      - name: 🎉 Pipeline Complete
        run: |
          cat << 'EOF'
          
          ╔════════════════════════════════════════════════════════════════════════╗
          ║                                                                        ║
          ║                    ✅ DEPLOYMENT SUCCESSFUL! ✅                         ║
          ║                                                                        ║
          ╚════════════════════════════════════════════════════════════════════════╝
          
          ┌────────────────────────────────────────────────────────────────────────┐
          │                           📊 DEPLOYMENT SUMMARY                         │
          ├────────────────────────────────────────────────────────────────────────┤
          │                                                                         │
          │  🎯 Status:        SUCCESS                                             │
          │  📦 Backend:       ✅ Built & Deployed                                 │
          │  🎨 Frontend:      ✅ Built & Deployed                                 │
          │  🗄️  Database:      ✅ Connected                                        │
          │  🖥️  VPS:           ✅ Running                                          │
          │                                                                         │
          ├────────────────────────────────────────────────────────────────────────┤
          │                           🌐 ACCESS INFORMATION                         │
          ├────────────────────────────────────────────────────────────────────────┤
          │                                                                         │
          │  🌍 Application:   http://185.229.224.232                              │
          │  🔌 API:           http://185.229.224.232/api                          │
          │  📊 Health Check:  http://185.229.224.232/api/health                   │
          │                                                                         │
          ├────────────────────────────────────────────────────────────────────────┤
          │                           📈 PIPELINE METRICS                           │
          ├────────────────────────────────────────────────────────────────────────┤
          │                                                                         │
          │  ⏱️  Total Time:    ~8-10 minutes                                       │
          │  🔄 Steps:         7/7 completed                                        │
          │  ✅ Success Rate:  100%                                                 │
          │                                                                         │
          └────────────────────────────────────────────────────────────────────────┘
          
          
                              🎊 READY FOR PRODUCTION! 🎊
          
          
          ════════════════════════════════════════════════════════════════════════
          
          EOF
          
          echo "🕐 Completed at: $(date '+%Y-%m-%d %H:%M:%S')"
          echo ""
          echo "📝 Next steps:"
          echo "   • Visit http://185.229.224.232 to see your app"
          echo "   • Check logs: ssh user@vps 'docker compose logs -f'"
          echo "   • Monitor: ssh user@vps 'docker stats'"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "                   🚀 CA2E IS LIVE! 🚀"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"clear